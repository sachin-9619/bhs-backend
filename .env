# Server
PORT=5000

# MySQL Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Sachin@4511
DB_NAME=bhstravels

ADMIN_USER=admin
ADMIN_PASS=admin123
# Email Configuration

MAIL_USER=bhs.it.services45@gmail.com
MAIL_PASS=rygiiswudhcmrkde

ADMIN_EMAIL=bhs.it.services45@gmail.com

app.get("/api/bookings", async (req, res) => {
  try {
    const [rows] = await db.execute(`
      SELECT b.id, b.userName, b.phone, b.seats, r.busName, r.departure, r.destination, r.departureTime
      FROM bookings b
      LEFT JOIN routes r ON b.route_id = r.id
      ORDER BY b.id DESC
    `);
    res.json(rows);
  } catch (err) {
    console.error(err);
    res.status(500).json({ error: "Server error" });
  }
});
VITE_API_BASE=https://bhs-backend-production.up.railway.app/api
